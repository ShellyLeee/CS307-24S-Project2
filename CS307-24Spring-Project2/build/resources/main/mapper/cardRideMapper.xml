<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cs307.cs30724springproject2.mapper.cardRideMapper">
    <insert id="insert" parameterType="cardRide">
        INSERT INTO cardRide (card_code, start_station, end_station, price, start_time, end_time)
        VALUES (#{card_code}, #{start_station}, #{end_station}, #{price}, #{start_time}, #{end_time})
    </insert>

    <!-- 多参数查询ride:card_code,start_station,end_station,price -->
    <select id="selectCardRideByParameters" resultType="cardRide">
        SELECT * FROM card_ride
        <where>
            <if test="param1 != null">
                AND column1 = #{param1}
            </if>
            <if test="param2 != null">
                AND column2 = #{param2}
            </if>
            <if test="param1 != null">
                AND column1 = #{param1}
            </if>
            <if test="param1 != null">
                AND column1 = #{param1}
            </if>
            <!-- 可以根据需要添加更多的条件 -->
        </where>
    </select>



</mapper>
